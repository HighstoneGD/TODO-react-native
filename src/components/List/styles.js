import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    container: {
        marginTop: 30,
        alignItems: 'stretch'
    },
    noItems: {
        textTransform: 'uppercase',
        marginTop: 10,
        alignSelf: 'center'
    }
})